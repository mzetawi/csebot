Optional production model: place csebot.glb here and set EXPERIENCE.modelEnabled=true.
Keep it centered at the origin, facing +Z, roughly 2.5 units high. Animation clip names match RobotPose; idle is the fallback. The procedural model is the shipped model, not a loading placeholder.
